apiVersion: v1
kind: ServiceAccount
metadata:
  name: obs-api
  namespace: apis
---
# Read capability for the observability path. Least privilege —
# get/list pods + read pod logs, prod namespace only.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: obs-api-read
  namespace: prod
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list"]
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: obs-api-read
  namespace: prod
subjects:
  - kind: ServiceAccount
    name: obs-api
    namespace: apis
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: obs-api-read
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: obs-api-spiffe-helper
  namespace: apis
data:
  spiffe-helper.conf: |
    agent_address = "/spiffe-workload-api/spire-agent.sock"
    cmd = ""
    cmd_args = ""
    daemon_mode = true
    jwt_svids = [
      {
        jwt_audience = "https://curity.localtest.me/oauth/v2/oauth-token"
        jwt_svid_file_name = "/run/spiffe/curity-actor.jwt"
      }
    ]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: obs-api
  namespace: apis
  labels:
    app: obs-api
spec:
  replicas: 1
  selector:
    matchLabels: { app: obs-api }
  template:
    metadata:
      labels: { app: obs-api }
    spec:
      serviceAccountName: obs-api
      volumes:
        - name: spiffe-workload-api
          csi:
            driver: csi.spiffe.io
            readOnly: true
        - name: spiffe-svids
          emptyDir:
            medium: Memory
        - name: spiffe-helper-config
          configMap:
            name: obs-api-spiffe-helper
      containers:
        - name: api
          image: ai-agents-demo/obs-api:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8084
          volumeMounts:
            - name: spiffe-svids
              mountPath: /run/spiffe
              readOnly: true
          env:
            - name: PORT
              value: "8084"
            - name: CURITY_ISSUER
              value: https://curity.localtest.me/oauth/v2/oauth-anonymous
            - name: CURITY_JWKS_URI
              value: https://curity.localtest.me/oauth/v2/oauth-anonymous/jwks
            - name: API_AUDIENCE
              value: obs-api
            - name: REQUIRED_SCOPES
              value: obs:read
            - name: TARGET_NAMESPACE
              value: prod
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector.observability.svc.cluster.local:4318
            - name: OTEL_SERVICE_NAME
              value: obs-api
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: service.namespace=apis
            - name: SPIFFE_SVID_PATH
              value: /run/spiffe/curity-actor.jwt
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8084
            initialDelaySeconds: 3
            periodSeconds: 5
        - name: spiffe-helper
          image: ghcr.io/spiffe/spiffe-helper:0.11.0
          args: ["-config", "/etc/spiffe-helper/spiffe-helper.conf"]
          volumeMounts:
            - name: spiffe-workload-api
              mountPath: /spiffe-workload-api
              readOnly: true
            - name: spiffe-svids
              mountPath: /run/spiffe
            - name: spiffe-helper-config
              mountPath: /etc/spiffe-helper
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
---
apiVersion: v1
kind: Service
metadata:
  name: obs-api
  namespace: apis
  labels:
    # Route Service traffic through the apis-waypoint Envoy so its
    # AuthorizationPolicy (k8s/istio/apis-l7-authz.yaml) is enforced.
    istio.io/use-waypoint: apis-waypoint
spec:
  selector:
    app: obs-api
  ports:
    - name: http
      port: 8084
      targetPort: 8084
